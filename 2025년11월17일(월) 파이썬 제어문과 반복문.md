# 파이썬 조건문과 반복문 정리

## 1️⃣ 조건문 (if, if-else, if-elif-else)

### 🔹 기본 if문
- 조건에 따라 코드 실행
```python
if 조건식:
    print("조건식이 True입니다.")
조건문 블록을 "True라면" 으로 읽으면 이해가 쉬움

🔹 비교 연산자

patient_age = 25

# ❌ 잘못된 예시 (대입 연산자 사용)
# if patient_age = 18:  # SyntaxError!

# ✅ 올바른 예시 (비교 연산자 사용)
if patient_age == 18:
    print("성인이 되었습니다!")

🔹 bool 자료형 활용
is_emergency = True

# ❌ 불필요한 비교
if is_emergency == True:
    print("응급상황입니다!")

# ✅ 올바른 사용
if is_emergency:
    print("응급상황입니다!")

# not 연산자
if not has_insurance:  # False이면 실행
    print("보험 정보를 확인해주세요")

🔹 if-else
patient_age = 16

if patient_age >= 18:
    print("성인용 약물을 처방합니다.")
else:
    print("소아용 약물을 처방합니다.")

🔹 if-elif-else

여러 조건 처리

current = "점심"

if current == "점심":
    print("치킨")
elif current == "저녁":
    print("샐러드")
else:
    print("기타")

2️⃣ 반복문
🔹 for문 - 정해진 횟수만큼 반복
patients = ["김환자", "이환자", "박환자"]

for patient in patients:
    print(patient)

🔹 유용한 반복문 함수
함수	용도	반환값	예시
range()	연속된 숫자 생성	숫자 시퀀스	range(5) → 0,1,2,3,4
enumerate()	인덱스와 값 함께	(인덱스, 값) 튜플	enumerate(['a','b']) → (0,'a'), (1,'b')
zip()	여러 리스트 동시 순회	각 리스트 요소를 튜플로 묶음	zip([1,2], ['a','b']) → (1,'a'), (2,'b')
🔹 enumerate() 예시
patients = ["김환자", "이환자", "박환자"]

for room_number, patient_name in enumerate(patients, 101):
    print(f"{room_number}호: {patient_name}")

🔹 zip() 예시
patient_names = ["김환자", "이환자", "박환자"]
patient_ages = [65, 45, 32]
departments = ["내과", "외과", "소아과"]

for name, age, dept in zip(patient_names, patient_ages, departments):
    print(f"{name} ({age}세) - {dept}")


길이가 다른 리스트는 짧은 리스트 기준으로 순회

🔹 range() 활용
range(끝)            # 0부터 끝-1
range(시작, 끝)       # 시작부터 끝-1
range(시작, 끝, 간격)  # 간격만큼 증가

3️⃣ 딕셔너리 반복
patient_info = {
    "name": "김환자",
    "age": 65,
    "blood_type": "A+",
    "department": "내과"
}

# 키만 반복
for key in patient_info:
    print(f"정보 항목: {key}")

# 값만 반복
for value in patient_info.values():
    print(f"정보 내용: {value}")

# 키와 값 모두 반복
for key, value in patient_info.items():
    print(f"{key}: {value}")

4️⃣ while문
count = 1

while count <= 5:
    print(f"현재 숫자: {count}")
    count += 1  # count = count + 1
print("카운트 완료!")

5️⃣ break & continue
for i in range(1, 11):
    if i == 5:
        print("5를 만났으니 종료!")
        break  # 반복문 완전 종료
    print(f"숫자: {i}")

6️⃣ 리스트 내포(List Comprehension)
patient_names = ["김환자", "이환자", "박환자"]

# 기본 방법
formal_names = []
for name in patient_names:
    formal_names.append(name + "님")

# 내포용법
formal_names = [name + "님" for name in patient_names]
print(formal_names)

7️⃣ 리스트 vs 딕셔너리 비교
항목	리스트(List)	딕셔너리(Dictionary)
모양	[ ]	{ }
구성	값들의 나열	키:값 쌍
접근 방식	번호(index)	이름(key)
길이(len)	항목 개수	키 개수
순서	있음	Python 3.7+ 부터 유지됨
용도	순서대로 많은 데이터 저장	구조화된 속성 저장
