# 🐍 파이썬 자료구조 기초 정리 (Day 2)

## 📘 목차
- 리스트 (List)
- 슬라이싱 (Slicing)
- 리스트 조작과 메소드
- 튜플 (Tuple)
- 딕셔너리 (Dictionary)
- 집합 (Set)

---

## 🧩 자료구조 개념
여러 개의 데이터를 효율적으로 관리하기 위해 공통점을 가진 데이터를 한 곳에 묶어서 다루는 것

```python
# 비효율적
wonder_dogs1 = "인쿠시"
wonder_dogs2 = "표승주"
wonder_dogs3 = "백채림"
wonder_dogs4 = "이나현"

# 효율적
wonder_dogs = ["인쿠시", "표승주", "백채림", "이나현"]
1️⃣ 리스트 (List)

순서 있음

변경 가능

중복 가능

표기법: []

활용 예시: 시간별 혈압 기록, 처방 순서
medications = ["아스피린", "메트포르민", "리시노프릴", "아토르바스타틴", "오메프라졸"]
print(medications[0])  # '아스피린'

# 슬라이싱
print(medications[1:3])  # ['메트포르민', '리시노프릴']
print(medications[::-1]) # 전체 역순

# 리스트 조작
blood_pressures = [120, 125, 130, 118]

# 요소 추가
blood_pressures.append(135)
blood_pressures.insert(1, 122)
blood_pressures.extend([128, 122, 119])
print(blood_pressures)
# [120, 122, 125, 130, 118, 135, 128, 122, 119]

# 요소 삭제
blood_pressures.remove(122)  # 값으로 삭제
blood_pressures.pop(0)       # 인덱스로 삭제
blood_pressures.clear()      # 전체 삭제
# del blood_pressures        # 변수 자체 삭제 (사용 시 주석 해제)

# 정보 확인
len(blood_pressures)
blood_pressures.index(130)
blood_pressures.count(122)
122 in blood_pressures

# 정렬
sorted(blood_pressures)
blood_pressures.sort()
blood_pressures.reverse()

# 리스트 → 문자열
departments = ["내과", "외과", "소아과", "산부인과"]
print(", ".join(departments))
print(" | ".join(departments))

# 리스트 연산
morning_meds = ["아스피린", "메트포르민"]
afternoon_meds = ["인슐린", "혈압약"]
daily_meds = morning_meds + afternoon_meds
print(daily_meds)

2️⃣ 튜플 (Tuple)

리스트와 비슷하지만 한 번 생성하면 변경 불가

괄호 () 사용, 괄호 생략 가능
patient_tuple = ("김환자", "65세")
patient_list = ["김환자", "65세"]

# 변경 가능/불가
patient_list[1] = "66세"   # 가능
# patient_tuple[1] = "66세"  # ❌ 에러 발생

# 괄호 없이 생성 가능
blood_types = "A", "B", "O", "AB"
print(type(blood_types))  # <class 'tuple'>

3️⃣ 딕셔너리 (Dictionary)

키(Key)와 값(Value) 쌍으로 데이터를 저장

순서 없음 → key로 접근
patient = {
    "name": "김환자",
    "age": 65,
    "diagnosis": "고혈압",
    "medications": ["아스피린", "메트포르민"]
}

# 접근
print(patient["name"])

# 추가/수정
patient["phone"] = "010-1234-5678"
patient["age"] = 66

# 삭제
patient.pop("diagnosis")
patient.popitem()
patient.clear()
# del patient

# 안전한 접근
patient.get("age")
patient.get("gender", "N/A")
"name" in patient

4️⃣ 집합 (Set)

중복 불가, 순서 없음

{} 사용
allergy_drugs_list = ["페니실린", "아스피린", "페니실린", "이부프로펜", "아스피린"]
allergy_set = set(allergy_drugs_list)
print(allergy_set)
# {'아스피린', '페니실린', '이부프로펜'}

✅ 오늘 핵심 요약

리스트, 튜플, 딕셔너리, 집합 구조와 차이 이해

리스트 조작, 슬라이싱, 메소드 활용 방법 습득

딕셔너리와 집합으로 데이터 안전하게 관리
