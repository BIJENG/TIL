🐍 객체지향 프로그래밍(OOP) 기초 정리
📌 객체지향 프로그래밍이란?

객체지향 프로그래밍(Object-Oriented Programming, OOP)은
**데이터(속성)**와 **그 데이터를 다루는 함수(기능)**를 하나로 묶어 관리하는 프로그래밍 방식입니다.

➡️ 즉, 대상(객체)을 중심으로 프로그램을 구성하는 방식

✔ 예시

환자라는 객체는 다음과 같이 구성됨:

속성(데이터): 이름, 나이, 병력

기능(함수): 진료받기(), 처방받기()

🎯 객체지향의 4대 특징
1. 캡슐화(Encapsulation)

데이터와 기능을 하나로 묶어 숨기고 보호하는 것
예) 환자의 개인정보는 권한이 있는 사람만 접근 가능

2. 상속(Inheritance)

부모 클래스의 속성과 기능을 자식 클래스에게 물려줌
예) "직원" → "의사", "간호사"로 확장

3. 다형성(Polymorphism)

같은 기능 호출이더라도 객체에 따라 다르게 동작하는 것
예) 치료하기() → 환자마다 치료 방법이 다름

4. 추상화(Abstraction)

복잡한 것 중 중요한 부분만 드러내고 불필요한 부분을 숨김
예) 의료 절차를 단순화하여 핵심 기능만 노출

⚙️ 절차지향 vs 객체지향
절차지향	객체지향
함수 중심	객체 중심
순차적으로 실행	객체 간 상호작용
데이터와 함수 분리	데이터 + 함수 묶음
규모가 커질수록 비효율	대규모 개발에 유리
🧱 클래스(Class)와 객체(Object)
✔ 클래스(Class)

객체를 만들기 위한 설계도

속성과 기능을 정의함
예) Patient 클래스

✔ 객체(Object)

클래스로부터 생성된 실제 실체(Instance)
예) patient1 = Patient()

📝 Python 클래스 네이밍 규칙

클래스명: PascalCase → MyPhone, UserProfile

변수명: snake_case → my_phone

메서드명: snake_case → get_name()

클래스 속성/메서드는 주로 명사 + 동사

🏗️ 생성자와 self
✔ 생성자 __init__()

객체가 만들어질 때 자동으로 실행됨

인스턴스 변수를 초기화함

def __init__(self, name, age, patient_id):
    self.name = name
    self.age = age
    self.patient_id = patient_id

✔ self란?

현재 객체 자기 자신을 가리킴

자바의 this와 유사

🐟 붕어빵 예제 — 실전 OOP
class FishBread:
    factory = "벤타"          # 클래스 변수
    total_baked = 0          # 모든 기계가 구운 총 량

    def __init__(self, dough="밀가루 반죽", filling="팥", roast="웰던"):
        """붕어빵 기계 초기 설정"""
        self.dough = dough
        self.filling = filling
        self.roast = roast
        self.total_made = 0  # 특정 기계가 구운 갯수

    def bake(self, count=1):
        """붕어빵을 count개 굽습니다."""
        print(f"{count}개 주문 완료!")
        print({"반죽": self.dough, "속": self.filling, "굽기": self.roast})

        self.total_made += count
        FishBread.total_baked += count

        baked_items = []
        for _ in range(count):  # count번 반복
            baked_items.append({
                "dough": self.dough,
                "filling": self.filling,
                "roast": self.roast
            })

        return baked_items

    def get_total_made(self):
        return self.total_made

🧬 상속(Inheritance)
❌ 상속 없이 작성 (중복 발생)
class Doctor:
    def __init__(self, name, department, specialty):
        self.name = name
        self.department = department
        self.specialty = specialty

    def introduce(self):
        return f"안녕하세요, {self.department}의 {self.name}입니다."


class Nurse:
    def __init__(self, name, department, shift):
        self.name = name
        self.department = department
        self.shift = shift

    def introduce(self):
        return f"안녕하세요, {self.department}의 {self.name}입니다."


→ 이름, 부서, introduce()가 중복됨

✔ 상속으로 개선 (중복 제거)
부모 클래스
class MedicalStaff:
    def __init__(self, name, department):
        self.name = name
        self.department = department

    def introduce(self):
        return f"안녕하세요, {self.department}의 {self.name}입니다."

    def work(self):
        return f"{self.name}이(가) 업무를 수행합니다."

자식 클래스 — 상속 문법
class Doctor(MedicalStaff):
    def __init__(self, name, department, specialty):
        super().__init__(name, department)  # 부모 생성자 호출
        self.specialty = specialty

class Nurse(MedicalStaff):
    def __init__(self, name, department, shift):
        super().__init__(name, department)
        self.shift = shift

인스턴스 생성
doctor = Doctor("김의사", "내과", "심장내과")

🧰 super() 함수란?

super()는 자식 클래스에서 부모 클래스의 메서드를 호출할 때 사용되는 함수.

예:
super().__init__(name, department)

📦 정리

OOP는 데이터와 기능을 객체로 묶어서 효율적으로 관리하는 방식

클래스는 설계도, 객체는 실제 인스턴스

생성자에서 초기값 설정

self는 객체 자신

상속을 사용하면 중복 제거 & 체계적인 구조 설계 가능

super()는 부모 기능을 가져올 때 사용
