📌 Pandas 기본 문법 & 데이터 전처리 정리
1. Google Colab에서 CSV 불러오기

from google.colab import drive
drive.mount('/content/drive')

import pandas as pd
from IPython.display import display

df = pd.read_csv("/content/drive/MyDrive/판다스/train.csv")

print("데이터 크기:", df.shape)
display(df.head())
df.info()
print(df.dtypes)

2. 열(column) 최소값 / 최대값 구하기
df["Glucose"].min()       # pandas 방식
min(df["Glucose"])        # 파이썬 내장 함수

3. 정렬 (Sorting)
df.sort_values(by="Age", ascending=True)   # 오름차순
df.sort_values(by="Score", ascending=False) # 내림차순

4. 컬럼 추가하기
✔ 방법 1: 조건식 직접 할당
df["Passed"] = df["Score"] >= 80

✔ 방법 2: loc 사용
df.loc[df["Score"] >= 80, "Level"] = "High"
df.loc[df["Score"] < 80, "Level"] = "Normal"

5. 컬럼 삭제
df.drop(columns=["Passed"], inplace=True)


inplace=True → 실제 DataFrame에 즉시 반영

inplace=False → 반영되지 않음(실행 결과만 보여줌)

6. 행 삭제
df.drop(index="b", inplace=True)

7. 조건 필터링 (Boolean Indexing)
high_score = df[df["Score"] >= 90]

8. 데이터 타입 확인
df["ID"].dtype


※ dtype는 "속성(attribute)"이므로 괄호() 필요 없음

9. 기초 통계 함수
df.min()
df.max()
df.mean()
df.std()
df.median()
df.sum()


특정 열만 선택해서 계산하려면:

df["Score"].mean()
df[["Age", "Score"]].mean()

📌 결측치 처리 (Missing Values)
10. 결측치 확인
df.isnull().sum()

11. 숨겨진 결측치(0 → NaN) 변환

특정 컬럼에 0이 "결측값 의미"일 경우:

import numpy as np

potential_zeros = ["Glucose", "BloodPressure", "SkinThickness", "Insulin", "BMI"]
df[potential_zeros] = df[potential_zeros].replace(0, np.nan)

✔ 일반 규칙

숫자형 결측치 → np.nan

문자형 결측치 → None

12. 결측행 삭제(dropna)
df_clean = df.dropna(subset=["Glucose", "BloodPressure"])


✔ subset에 지정된 컬럼 중 하나라도 NaN이면 해당 행(row)을 삭제

13. 결측치 채우기(fillna)
1) 숫자형 컬럼만 선택
numeric_cols = df.select_dtypes(include=["int64", "float64"]).columns

2) 숫자형 컬럼 결측치를 평균으로 채우기
df[numeric_cols] = df[numeric_cols].fillna(df[numeric_cols].mean())

