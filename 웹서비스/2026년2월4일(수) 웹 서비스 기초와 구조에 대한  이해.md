# 🌐 웹 서비스 기초와 구조 (Web Service Basics)

## 1. 웹 서비스란?
인터넷을 기반으로 표준 규약(HTTP)을 통해 자원과 정보를 제공하는 시스템입니다.
- **특징:** 플랫폼 독립적(OS 상관없음), 연결성, 표준 프로토콜 사용

## 2. 웹의 동작 원리 (Client-Server Model)
웹은 기본적으로 **요청(Request)**과 **응답(Response)**의 구조로 작동합니다.

- **클라이언트 (Client):** "주문하는 손님" (브라우저, 앱) - 요청을 보냄
- **서버 (Server):** "요리하는 주방장" - 요청을 처리하고 응답을 보냄
- **데이터베이스 (DB):** "식재료 창고" - 데이터를 저장하고 관리

> **흐름:** 사용자 행동 → 클라이언트 요청 → 서버 처리 (DB 조회) → 서버 응답 → 클라이언트 화면 반영

## 3. HTTP (HyperText Transfer Protocol)
웹에서 클라이언트와 서버가 서로 대화하기 위한 **공통된 통신 규칙(언어)**입니다.

### ✉️ HTTP 메시지 구조
- **Header:** 데이터의 형식, 길이, 인증 정보 등 (택배 운송장)
- **Body:** 실제 전달할 데이터 (택배 상자 내용물)

### 🚦 HTTP 상태 코드 (Status Code)
서버가 요청 처리 결과를 숫자로 알려주는 것
- `2xx` (성공): **200 OK** (잘 됐어!), **201 Created** (만들어졌어!)
- `4xx` (클라이언트 오류): **400 Bad Request** (형식 틀렸어), **404 Not Found** (그런 거 없어)
- `5xx` (서버 오류): **500 Internal Server Error** (내 실수야, 미안)

## 4. API와 REST API
### API (Application Programming Interface)
프로그램끼리 소통하기 위한 **약속된 인터페이스**입니다.

### REST API
자원(Resource)을 중심으로 HTTP 메서드를 활용하는 API 설계 방식입니다.
- **자원 (URL):** 명사로 표현 (예: `/users`, `/products`)
- **행위 (Method):** 동사로 표현 (CRUD)

| 행위 | HTTP 메서드 | 설명 |
|---|---|---|
| **C**reate | `POST` | 데이터 생성 (회원가입, 글쓰기) |
| **R**ead | `GET` | 데이터 조회 (목록 보기, 검색) |
| **U**pdate | `PUT` / `PATCH` | 데이터 수정 (정보 변경) |
| **D**elete | `DELETE` | 데이터 삭제 (탈퇴, 지우기) |

## 5. HTTPS와 보안 (Security)
HTTP는 평문 통신이라 도청 위험이 있어, 이를 암호화한 것이 **HTTPS (HTTP + TLS)**입니다.

### 🔐 암호화 방식 (하이브리드)
1. **공개키 방식 (비대칭키):** 처음 연결할 때 **대칭키**를 안전하게 교환하기 위해 사용 (느리지만 안전함)
2. **대칭키 방식:** 실제 데이터를 주고받을 때 사용 (빠름)

> **TLS 핸드셰이크 요약:**
> "서버야 안녕?" → "인증서 줄게(공개키 포함)" → (클라이언트가 인증서 확인) → "이 대칭키를 네 공개키로 잠궈서 보낼게" → (서버가 비밀키로 엶) → "OK, 이제부터 이 대칭키로 대화하자!"

## 6. Keep-alive (성능 최적화)
HTTP는 원래 요청 한 번에 연결 한 번을 맺고 끊는 구조(비연결성)였습니다. 하지만 이는 비효율적입니다.

### ⚡ Keep-alive란?
하나의 TCP 연결(Connection)을 끊지 않고 유지하면서, **여러 개의 HTTP 요청/응답을 처리**하는 기술입니다.

### 왜 사용하는가?
- **연결 비용 절약:** 매번 3-way handshake(연결 수립)와 TLS handshake(보안 인증)를 다시 할 필요가 없음.
- **성능 향상:** 지연 시간(Latency)이 줄어들고 서버 부하가 감소함.

```http
// Keep-alive 예시 헤더
Connection: keep-alive
Keep-Alive: timeout=5, max=1000
```
